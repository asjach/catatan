## clone repository git
`git clone url`

## config email dan user
- `git config --global user.email "you@example.com"`
- `git config --global user.name "Your Name"`
- setelah itu akan muncul konfirmasi untuk login ke github (ikuti langkahnya)